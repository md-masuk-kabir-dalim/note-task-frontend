export enum tagTypes {
  auth = "auth",
  users = "users",
  notes = "notes",
  posts = "posts",
}

export const tagTypesList = [
  tagTypes.auth,
  tagTypes.users,
  tagTypes.notes,
  tagTypes.posts,
];
